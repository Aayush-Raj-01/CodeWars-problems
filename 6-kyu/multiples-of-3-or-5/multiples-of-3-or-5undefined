int solution(int number) 
{
  int arr[100] = {0},j=0,i=0;
for(int i = 1;i<number;i++){
  if(i%3==0){
    arr[j]=i;
    j++;
  }
  else if(i%5==0){
    arr[j]=i;
    j++;
  }
  }
  int sum =0;
  for(int i=0;i<j;i++){
    sum = sum +arr[i];
  }
  return (sum>0 ? sum : 0);
}