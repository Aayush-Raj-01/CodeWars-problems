int square_digits(int num) {
  int n;
    if (num==0) return 0;
  
  std::string re = "";
  while(num!=0){
    n = num%10;
    re = std::to_string(n*n) + re;
      num = num/10;
  }
  return stoi(re);
}
​